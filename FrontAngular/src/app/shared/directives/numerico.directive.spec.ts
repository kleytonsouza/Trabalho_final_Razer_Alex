import { NumericoDirective } from './numerico.directive';

xdescribe('NumericoDirective', () => {
  it('should create an instance', () => {
    const directive = NumericoDirective;
    expect(directive).toBeTruthy();
  });
});
